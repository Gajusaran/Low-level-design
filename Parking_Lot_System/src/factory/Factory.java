package factory;

public class Factory {
    // where we use this facotry pattern -
//    PricingStrategy pricingStrategy =
//            PricingStrategyFactory.getStrategy(vehicleType);
//
//    PaymentStrategy paymentStrategy =
//            PaymentStrategyFactory.getStrategy(paymentType);
//
//    ParkingService parkingService =
//            new ParkingService(parkingLot, ticketRepo,
//                    pricingStrategy, paymentStrategy);
    // this is how we use factory in client

    // public class VehicleFactory {
    //    public static Vehicle createVehicle(VehicleType type) {
    //        return switch (type) {
    //            case CAR -> new Car();
    //            case BIKE -> new Bike();
    //            case TRUCK -> new Truck();
    //        };
    //    }
    //}

}
